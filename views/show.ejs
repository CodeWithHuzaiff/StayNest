<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title>StayNest | Listing</title>
  <link rel="stylesheet" href="/css/show.css">
</head>

<body>
  <div class="listing-card">
    <img src="<%= attach.image.url %>" alt="Listing image" />

    <div class="card-content">
      <h2><%= attach.title %></h2>

      <p><%= attach.description %></p>

      <div class="meta">
        📍 <%= attach.location %>, <%= attach.country %>
      </div>

      <div class="price">
        ₹ <%= attach.price.toLocaleString("en-IN") %> / night
      </div>

      <div class="actions">
        <form method="get" action="/listings/<%= attach._id %>/edit">
          <button class="edit-btn">Edit</button>
        </form>

        <form method="post" action="/listings/<%= attach._id %>?_method=DELETE">
          <button class="delete-btn">Delete</button>
        </form>

        <form method="get" action="/listings">
          <button class="back-btn">Back</button>
        </form>
      </div>
    </div>
  </div>
</body>
</html>